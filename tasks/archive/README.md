# tasks/archive

Old planning and design docs kept for historical reference only.
The contents may describe state, commands, or APIs that no longer
match the shipped CLI.

Do not read these for current behavior — use:

- `mn help` / `mn help <command>` — current commands and flags
- `mn manual` — full reference manual
- `mn help --agent` — agent-oriented reference
- `docs/SKILL.md` — agent skill served via MCP
- `CHANGELOG.md` — what shipped when
